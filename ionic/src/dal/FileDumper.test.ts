import { FileDumper } from './FileDumper'


// TODO: This browser doesn't support IndexedDB
describe('FileDumper', function() {
    it('should make a temporary file', async function() {
        const instance = new FileDumper();
        //const lhs = await instance.readDump("test-read-dump-file.bak");
        //expect(lhs).toStrictEqual(undefined);
        //const actual = await instance.dump("test-read-dump-file.bak", "");
        //expect(actual).toStrictEqual(undefined);
        //const rhs = await instance.readDump("test-read-dump-file.bak");
        //expect(rhs).toStrictEqual(undefined);
    });
});
